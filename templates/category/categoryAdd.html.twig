{% extends 'base.html.twig' %}

{%  block body %}


    {{ form_start(form) }}
    {{ form_widget(form.name) }}
    <input type="submit" value="Envoyer" />
    {{ form_end(form) }}

    <hr>
    <a href="{{ path("categorynew") }}">
        Back to all category
    </a>

{%  endblock %}
